<% if @patient.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
      The form contains <%= pluralize(@patient.errors.count, "error") %>.<ul>
    <% @patient.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
